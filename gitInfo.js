/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

//CODE HERE
console.log("----------------------------------")
let gitDefinition = "Git a program that allows users to save different versions of their code in"+
    "different timespans called snapshots.Hence why it is a version control system. This enables tracking changes"+
    "and viewing the entire project history. It is stored in repo's which is a code project held in a sigle folder "+
    "named the directory.";
console.log(gitDefinition)
//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

//CODE HERE
console.log("----------------------------------")
let gitHubDefinition="Github is a website owned by Microsoft that enables users to save the local system git"+
"repos and upload/clone. It is the cloud for the git repos on the local system. Users can clone repos from GitHub"+
"and move it to their local machine or commit their local machine repos onto GitHub using push";
console.log(gitHubDefinition)
//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

//CODE HERE
console.log("----------------------------------")
let gitInitDefinition = "git init is a command in the terminal that allows users to initialize a new repo for their code or "+
"if it already exists reinitializes the existing one";
console.log(gitInitDefinition)
//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

//CODE HERE
console.log("----------------------------------")
let gitCloneDefinition = "git clone is a command in the terminal that allows users to clone code from GitHub cloud"+
"and store it on their local machine. So essentially creates a copy of that repo/branch";
console.log(gitCloneDefinition)
//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

//CODE HERE
console.log("----------------------------------")
let gitStatusDefinition = "git status is a command in the terminal that allows users to see the current state of thier"+
"directory and staging area. Gives users details such as where the HEAD is, if any changed files were not committed.";
console.log(gitStatusDefinition)
//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

//CODE HERE
console.log("----------------------------------")
let gitAddDefinition =  "git add is a command in the terminal that allows users to add new or changed files to the git staging"+
"area. It is used before committing to see what you are going to commit."
console.log(gitAddDefinition)
//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

//CODE HERE
console.log("----------------------------------")
let gitCommitDefinition = "git commit is a command in the terminal that allows users to create a snapshot of their code in the repo."+
"It allows the different versions of your code to be saved.";
console.log(gitCommitDefinition)
//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

//CODE HERE
console.log("----------------------------------")
let gitPushDefinition = "git push is a command in the terminal that allows users to upload the local branch commit to the"+
"corresponding remote branch. It will update/ publish the repo onto the remote branch, in our case, GitHub";
console.log(gitPushDefinition)